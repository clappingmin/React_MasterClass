import { DefaultTheme } from './styled.d';

const lightTheme: DefaultTheme = {
  bgColor: 'white',
  textColor: 'black',
};

const darkTheme: DefaultTheme = {
  bgColor: 'black',
  textColor: 'white',
};
